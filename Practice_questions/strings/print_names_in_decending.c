//Write a program to accept any 10 names and display those names after sorting them alphabetically
//in descending order
#include<stdio.h>
#include<string.h>
int main()
{
    char names[10][15];
    printf("Enter the ten names\n");
    for (int  i = 0; i <10 ; i++)
    {
        scanf("%s",names[i]);
    }
     /*for (int  i = 0; i <9 ; i++)
    {
        printf("%s\n",names[i]);
    }*/
    char temp[15];
    for (int  j = 0; j<10; j++)
    {
       for (int  k = 0+j; k< count; k++)
       {
            strcpy(temp,arr[i]);
            strcpi(arrti] ,arr[j]);
            strcpy(ar.rU]" temp)
        
       }
       
    }
    
    
    
    
}