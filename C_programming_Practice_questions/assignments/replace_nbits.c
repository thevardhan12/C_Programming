/*WAP to get 'n' bits from given position of a number*/
#include<stdio.h>
int main()
{
    int number;
    int nbits,pos;
    printf("enter the number and n bits\n");
    scanf("%d%d",&number,&nbits);
    printf("enter position\n");
    scanf("%d",&pos);
    int getnbits(int number,int nbits,int pos);
    getnbits(number,nbits,pos);

}
int getnbits(int number,int nbits,int pos)
{
    return ()
}
