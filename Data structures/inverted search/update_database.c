#include"hash.h"
extern int flag;
status update_database()
{
    int flag=1;
    if(flag!=1)
    {

    }
    //read the filename open in read mode
    //check the 1st charcter is # if yes proceed else
    //error exit

}