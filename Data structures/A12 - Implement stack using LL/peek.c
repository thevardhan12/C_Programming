#include "stack.h"

int Peek(Stack_t **top)
{
    Stack_t *temp;          //take temp pointer
    temp=*top;
    if(*top==NULL)
    {
        return FAILURE;
    }
    else
    {
        
        

    }

}